{
  "name": "tomer-app",
  "version": "1.0.0",
  "main": "client/main.js",
  "scripts": {
    "client": "cd client && npm start",
    "server": "cd server && npm start",
    "electron": "wait-on http://localhost:3000 && ./client/node_modules/.bin/electron .",
    "dev": "concurrently --names \"SERVER,CLIENT,DESKTOP\" --prefix-colors \"bgBlue.bold,bgGreen.bold,bgMagenta.bold\" \"npm run server\" \"npm run client\" \"npm run electron\"",
    "build": "cd client && npm run build && cd ../server && npm run build"
  },
  "author": "Tomer Shatz",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "wait-on": "^9.0.4"
  }
}
